# API URL for the backend (Render URL or localhost for development)
# Note: If you include /api at the end, the frontend is configured to handle it correctly.
VITE_API_URL=https://evangadi-forum-backend-osvc.onrender.com/api
